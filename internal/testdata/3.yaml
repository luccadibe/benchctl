benchmark:
  name: bad-healthcheck
  output_dir: ./results

hosts:
  local: {}

stages:
  - name: bad-hc
    host: local
    command: echo "hello"
    health_check:
      type: unknown
      target: anything
      timeout: -5s
      retries: -1

